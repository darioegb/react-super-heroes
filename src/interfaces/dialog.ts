export interface ConfirmDialogConfig {
  /**
   * Modal title
   */
  title: string;
  /**
   * Optional cancel button text. By default is Cancel
   */
  cancelButtonText?: string;
  /**
   * Optional confirm button text. By default is Confirm
   */
  confirmButtonText?: string;
}
